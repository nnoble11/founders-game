# Form backend (deferred decision — swap-friendly).
# When unset, applications are logged server-side and the form still succeeds,
# so the site can ship before the backend is wired up.
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
AIRTABLE_TABLE_NAME=Applications
